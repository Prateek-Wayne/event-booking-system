apiVersion: v1
kind: Service
metadata:
  name: {{ include "event-booking-system-chart.fullname" . }}
  labels:
    {{- include "event-booking-system-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "event-booking-system-chart.selectorLabels" . | nindent 4 }}
